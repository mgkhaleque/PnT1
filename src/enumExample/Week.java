package enumExample;

/* Enumeration serves the purpose of representing a group of named constants in a programming language
 * enum can declare multiple Pre-defined, Constant data
 * which Class or Interface can not do
 * natural enumerated types: (like the planets, days of the week, months of the year, colors, directions, etc.).
 * Every enum constant represents an object of enum type (e.g. here, Week is enum constant & day is object of enum type
 * enum type can be passed as an argument to switch statement.
 *

 */

//Declaring predefined-constant data in enum

public enum Week {
    Monday,Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday,NoDay

}
